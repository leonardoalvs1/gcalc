// src/screens/MeasurementInputScreen/style.js (VERSÃO ATUALIZADA)
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    backgroundColor: '#f8f9fa', // Cor de fundo da tela
    paddingTop: 20, // Espaçamento superior para o título
  },
});

export default styles;