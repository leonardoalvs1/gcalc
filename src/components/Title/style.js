// src/components/Title/style.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    // Estilos para o contêiner do título
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 10,
  },
  text: {
    fontSize: 32, // Tamanho maior para o título principal
    color: '#007bff', // Cor de destaque
    fontWeight: 'bold',
    textAlign: 'center',
  },
});

export default styles;